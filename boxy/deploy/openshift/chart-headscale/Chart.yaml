apiVersion: v2
name: chart-headscale
description: Headscale (self-hosted Tailscale control server) on OpenShift — the Tier-2 MagicDNS naming authority behind `boxy open --publish`.
type: application
version: 0.0.1
appVersion: "0.23.0"
maintainers:
  - name: boxy
