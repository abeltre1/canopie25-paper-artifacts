# Flux + CUDA GPU cluster.
[location]
name = "flux-cuda"
scheduler = "flux"
accelerator = "cuda"
runtime = "podman"
offline = true
[location.resources]
gpus_per_node = 4
gpu_vram_gb = 80    # A100/H100-class parts; the model-card geometry solver sizes against this
