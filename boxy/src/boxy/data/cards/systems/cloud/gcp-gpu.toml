# Cloud GPU on GCP via SkyPilot (H100). boxy generate sky --location <this>.
[location]
name = "cloud-gcp-gpu"
scheduler = "none"
accelerator = "cuda"
[location.resources]
gpus_per_node = 1
accelerator_type = "H100"
