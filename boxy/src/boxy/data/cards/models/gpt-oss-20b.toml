# openai/gpt-oss-20b — MXFP4 MoE, fits a single 80GB-class GPU.
[model]
match = "openai/gpt-oss-20b*"
engine = "vllm"
gpus = 1
min_vram_gb = 20
[model.args]
# cap KV-cache profiling so the MoE fits its single GPU (override for more).
max_model_len = 8192
